<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title || 'Default title' }}</title>
    <meta name="description" content="{{ description || '' }}">

    @vite(['resources/js/app.js'])
  </head>

  <body>
    @!navigation()
    <div id="content-wrapper" data-mata-enter="bounceInLeft" data-mata-leave="bounceOutLeft">
      {{{ await $slots.main() }}}
    </div>
  </body>

</html>
